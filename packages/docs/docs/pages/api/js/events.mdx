# Events

## onUserUpdate

This gets called on every Auth change. If `user` is undefined, they are logged out. If it is defined, they are logged in.

```ts twoslash
import { onUserUpdate } from '@happychain/js'

onUserUpdate((user) => {
  //
})
```
