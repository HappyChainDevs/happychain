# Type Alias: EventBusOptions

```ts
type EventBusOptions: {
  logger: Logger;
  onError: (...params: unknown[]) => void;
  scope: string;
 } & {
  mode: IframePort;
  target: Window;
 } | {
  mode: DappPort;
 } | {
  mode: Broadcast;
 } | {
  mode: Forced;
  port: MessagePort | BroadcastChannel;
};
```

## Type declaration

### logger?

```ts
optional logger: Logger;
```

### onError()?

```ts
optional onError: (...params: unknown[]) => void;
```

#### Parameters

• ...**params**: `unknown`\[]

#### Returns

`void`

### scope

```ts
scope: string;
```

## Defined in

[packages/sdk-shared/lib/services/eventBus/eventBus.ts:45](https://github.com/HappyChainDevs/happychain/blob/cf58ffaf96112a54bbefeea34e6c68ce7f561403/packages/sdk-shared/lib/services/eventBus/eventBus.ts#L45)
