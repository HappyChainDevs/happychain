# Function: register()

```ts
function register(opts: WalletRegisterOptions): void
```

Defined in: [packages/core/lib/register.ts:54](https://github.com/HappyChainDevs/happychain/blob/99d9c499d60929c60f5b0529191c07ec6adb3faa/packages/core/lib/register.ts#L54)

Registers the required components and initializes the SDK

## Parameters

### opts

[`WalletRegisterOptions`](/sdk/react/api/type-aliases/WalletRegisterOptions) = `{}`

## Returns

`void`

## Examples

Connect to HappyChain Sepolia

```ts twoslash
import { register } from '@happy.tech/core'
// ---cut---
register()
```

Connect to a pre-defined chain

```ts twoslash
import { register } from '@happy.tech/core'
import { happyChainSepolia } from '@happy.tech/core'
// ---cut---
register({ chainId: happyChainSepolia.id })
```

Connect to a custom chain

```ts twoslash
import { register } from '@happy.tech/core'
// ---cut---
register({ chainId: "0x7a69" }) // in hex format
```
