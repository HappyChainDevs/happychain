# Function: useHappyChain()

```ts
function useHappyChain(): {
  connect: () => Promise<void>;
  disconnect: () => Promise<void>;
  initialized: boolean;
  loadAbi: (contractAddress: `0x${string}`, abi: Abi) => Promise<void>;
  provider: HappyProvider;
  requestSessionKey: (contractAddress: `0x${string}`) => Promise<void>;
  showSendScreen: () => void;
  user: undefined | HappyUser;
}
```

Defined in: [packages/react/lib/components/HappyWalletProvider.tsx:39](https://github.com/HappyChainDevs/happychain/blob/99d9c499d60929c60f5b0529191c07ec6adb3faa/packages/react/lib/components/HappyWalletProvider.tsx#L39)

## Returns

```ts
{
  connect: () => Promise<void>;
  disconnect: () => Promise<void>;
  initialized: boolean;
  loadAbi: (contractAddress: `0x${string}`, abi: Abi) => Promise<void>;
  provider: HappyProvider;
  requestSessionKey: (contractAddress: `0x${string}`) => Promise<void>;
  showSendScreen: () => void;
  user: undefined | HappyUser;
}
```

### connect()

```ts
() => Promise<void>
```

### disconnect()

```ts
() => Promise<void>
```

### initialized

```ts
initialized: boolean;
```

### loadAbi()

```ts
(contractAddress: `0x${string}`, abi: Abi) => Promise<void>
```

### provider

```ts
provider: HappyProvider = happyProvider;
```

### requestSessionKey()

```ts
(contractAddress: `0x${string}`) => Promise<void>
```

### showSendScreen()

```ts
() => void
```

### user

```ts
user: undefined | HappyUser;
```
