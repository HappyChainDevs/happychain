# Type Alias: AddEthereumChainParameter

```ts
type AddEthereumChainParameter = {
  blockExplorerUrls: string[];
  chainId: string;
  chainName: string;
  iconUrls: string[];
  nativeCurrency: {
     decimals: number;
     name: string;
     symbol: string;
    };
  rpcUrls: readonly string[];
};
```

Defined in: node\_modules/viem/\_types/types/eip1193.d.ts:43

## Type declaration

### blockExplorerUrls?

```ts
optional blockExplorerUrls: string[];
```

### chainId

```ts
chainId: string;
```

A 0x-prefixed hexadecimal string

### chainName

```ts
chainName: string;
```

The chain name.

### iconUrls?

```ts
optional iconUrls: string[];
```

### nativeCurrency?

```ts
{
  decimals: number;
  name: string;
  symbol: string;
}
```

Native currency for the chain.

### rpcUrls

```ts
rpcUrls: readonly string[];
```
