# Type Alias: EIP1193ErrorObject

```ts
type EIP1193ErrorObject = {
  code: ProviderRpcErrorCode;
  data: unknown;
  message: string;
};
```

Defined in: [support/wallet-common/lib/errors/interfaces.ts:14](https://github.com/HappyChainDevs/happychain/blob/99d9c499d60929c60f5b0529191c07ec6adb3faa/support/wallet-common/lib/errors/interfaces.ts#L14)

Error Object is used to transmit error messages
across MessageChannel and BroadcastChannels.
This requires the data to be JSON serializable
so we can't send the raw Error class

## Type declaration

### code

```ts
code: ProviderRpcErrorCode;
```

### data?

```ts
optional data: unknown;
```

### message

```ts
message: string;
```
