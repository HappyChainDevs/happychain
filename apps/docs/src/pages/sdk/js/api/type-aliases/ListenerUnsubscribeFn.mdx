# Type Alias: ListenerUnsubscribeFn()

```ts
type ListenerUnsubscribeFn = () => void;
```

Defined in: [packages/core/lib/happyProvider/listeners.ts:35](https://github.com/HappyChainDevs/happychain/blob/99d9c499d60929c60f5b0529191c07ec6adb3faa/packages/core/lib/happyProvider/listeners.ts#L35)

Cleanup function which can be used to unsubscribe from the event which it was returned from.

## Returns

`void`

## Example

```ts
useEffect({
  const unsubscribe = onUserUpdate(user => console.log({ user }))
  return () => unsubscribe()
}, [])
```
